<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deundeun.pay.mapper.ChargeLotAllocationMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO charge_lot_allocations (charge_lot_id, wallet_id, reference_id, amount)
        VALUES (#{chargeLotId}, #{walletId}, #{referenceId}, #{amount})
    </insert>

    <select id="selectByWalletIdAndReferenceId" resultType="com.deundeun.pay.domain.ChargeLotAllocation">
        SELECT id, charge_lot_id, wallet_id, reference_id, amount, created_at
        FROM charge_lot_allocations
        WHERE wallet_id = #{walletId} AND reference_id = #{referenceId}
    </select>

</mapper>
