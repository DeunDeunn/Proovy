<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.deundeun.ai.mapper.AiTicketMapper">

    <select id="findActivePlans" resultType="com.deundeun.ai.vo.AiTicketPlanVo">
        SELECT id,
               name,
               duration_days,
               price,
               description,
               active
        FROM ai_ticket_plans
        WHERE active = true
        ORDER BY duration_days ASC, price ASC, id ASC
    </select>

    <select id="findPlanById" resultType="com.deundeun.ai.vo.AiTicketPlanVo">
        SELECT id,
               name,
               duration_days,
               price,
               description,
               active
        FROM ai_ticket_plans
        WHERE id = #{planId}
    </select>

    <select id="findActiveSubscriptionByHostId" resultType="com.deundeun.ai.vo.AiTicketSubscriptionVo">
        SELECT s.id,
               s.host_id,
               s.plan_id,
               p.name AS plan_name,
               s.paid_price,
               s.started_at,
               s.expired_at,
               s.status,
               s.created_at,
               s.updated_at
        FROM ai_ticket_subscriptions s
        JOIN ai_ticket_plans p ON p.id = s.plan_id
        WHERE s.host_id = #{hostId}
          AND s.status = 'ACTIVE'
          AND s.started_at <= now()
          AND s.expired_at > now()
        ORDER BY s.expired_at DESC, s.id DESC
        LIMIT 1
    </select>

    <insert id="insertSubscription" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_ticket_subscriptions (
            host_id,
            plan_id,
            paid_price,
            started_at,
            expired_at,
            status
        ) VALUES (
            #{hostId},
            #{planId},
            #{paidPrice},
            #{startedAt},
            #{expiredAt},
            #{status}
        )
    </insert>

    <insert id="insertTicketHistory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_tickets (
            host_id,
            subscription_id,
            type
        ) VALUES (
            #{hostId},
            #{subscriptionId},
            #{type}
        )
    </insert>

</mapper>
