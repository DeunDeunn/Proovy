<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deundeun.pay.mapper.HostRevenueMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO host_revenues (
        host_id, challenge_id, settlement_id, amount, status, paid_at
        )
        VALUES (
        #{hostId}, #{challengeId}, #{settlementId},
        #{amount}, #{status}, #{paidAt}
        )
    </insert>

    <select id="selectByChallengeId" resultType="com.deundeun.pay.dto.HostRevenueItem">
        SELECT id, challenge_id, amount, status, paid_at
        FROM host_revenues
        WHERE challenge_id = #{challengeId}
    </select>

    <select id="selectByHostId" resultType="com.deundeun.pay.dto.HostRevenueItem">
        SELECT id, challenge_id, amount, status, paid_at
        FROM host_revenues
        WHERE host_id = #{hostId}
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByHostId" resultType="long">
        SELECT COUNT(*)
        FROM host_revenues
        WHERE host_id = #{hostId}
    </select>

    <select id="existsByChallengeIdAndHostId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM host_revenues
            WHERE challenge_id = #{challengeId} AND host_id = #{hostId}
        )
    </select>
</mapper>